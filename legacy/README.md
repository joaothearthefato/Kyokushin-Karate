# Arquivos legados

Estes arquivos não possuem referências ativas no código atual e foram preservados para consulta histórica. Não inclua novos imports ou requires a partir desta pasta.

- `php/navbar.php`: navbar alternativa antiga; a aplicação usa `includes/navbar.php`.
- `css/perfil_aluno.css`: estilo de perfil sem referência ativa.
- `css/registro.css`: estilo antigo de registro com referência a imagem inexistente.
- `sql/schema.v2.5.sql`: schema histórico; a fonte atual é `database/schema.sql`.
